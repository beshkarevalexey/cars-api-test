services:
  api:
    build: .
    ports:
      - "3000:3000"

  frontend:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - ./public:/app
      - ./start.sh:/app/start.sh
    ports:
      - "3001:3001"
    command: ["sh", "start.sh"]
